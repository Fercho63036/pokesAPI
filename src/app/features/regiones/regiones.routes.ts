import { Routes } from '@angular/router';
import { RegionesComponent } from './components/regiones.component';

export const REGIONES_ROUTES: Routes = [
    {
        path: '',
        component: RegionesComponent
    }
];